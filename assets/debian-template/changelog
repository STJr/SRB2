${PACKAGE_NAME}-data (${PROGRAM_VERSION}-${__PACKAGE_DATETIME_DIGIT}) ${PACKAGE_DISTRO}; urgency=high

  * ${PROGRAM_NAME} v${PROGRAM_VERSION} asset data

 -- ${PACKAGE_NAME_EMAIL}  ${__PACKAGE_DATETIME}
