${PACKAGE_NAME} (${PROGRAM_VERSION}-${__PACKAGE_DATETIME_DIGIT}) ${PACKAGE_DISTRO}; urgency=high

  * ${PROGRAM_NAME} v${PROGRAM_VERSION} program build

 -- ${PACKAGE_NAME_EMAIL}  ${__PACKAGE_DATETIME}
